# ─── EEG from-scratch training pipeline ──────────────────────────────────────
# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tqdm>=4.65
PyYAML>=6.0
python-dotenv>=1.0

# Deep learning
torch>=2.1

# EEG deep learning (EEGConformer)
braindecode>=1.3.2

# Experiment tracking (optional — set wandb.enabled: true in config to use)
wandb>=0.17

# LEAD dataset download helper
gdown>=5.1

# HuggingFace Hub (model upload)
huggingface_hub>=0.20

# LLM reasoning layer (clinician-style report generation)
anthropic>=0.25

# Google Sheets experiment tracker
gspread>=6.0
google-auth>=2.28

# Visualization
matplotlib>=3.7
seaborn>=0.13

# KOA HPC tooling
-e ./koa-cli

# Development / testing
pytest>=7.4
black>=24.0
isort>=5.13
